#ifndef FAKELOG_H_INCLUDED
#define FAKELOG_H_INCLUDED

#define LOG(...)
#define LOGV(...)
#define LOGD(...)
#define LOGI(...)
#define LOGW(...)
#define LOGE(...)
#define LOG_FATAL_IF(...)
#define LOG_ALWAYS_FATAL_IF(...)
#define LOG_ASSERT(...)
#define LOG_ALWAYS_FATAL(...)

#define SLOGE(...)
#define SLOGW(...)

#endif // FAKELOG_H_INCLUDED
